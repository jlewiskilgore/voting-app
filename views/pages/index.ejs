<html>
<head>
  <title>Voting App</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <h1 id="page-title">Voting App</h1>
  <br>
  <br>
  <div id="create-poll-button">
    <button onclick="window.location.href='/createPoll'">Create a New Poll</a>
  </div>
  <br>
  <br>
  <div id="poll-list">
    <% for (var i=0; i < pollList.length; i++) { %>
      <form action="/viewPoll" method="post">
        <input type="hidden" name="id" value="<%= pollList[i]._id %>">
        <input type="hidden" name="question" value="<%= pollList[i].question %>">
        <input type="hidden" name="choices" value="<%= pollList[i].choices %>">
    	  <input type="submit" value="<%= pollList[i].question %>">
      </form>
    <br>
    <% } %>
  </div>
</body>
</html>